// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "common/src/**/*.js": true,
        "common/src/**/*.d.ts": true,
        "**/*.map": true
    }
}